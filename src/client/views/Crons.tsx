// ─── Crons ──────────────────────────────────────────────────────────────────
// Cron job list with status, next-run, search
// Implemented in PR 5

export function Crons() {
  return (
    <div>
      <h2 className="text-lg font-semibold text-[var(--fg2)] mb-4">Cron Jobs</h2>
      <p className="text-[var(--fg3)]">Cron view implemented in PR 5.</p>
    </div>
  );
}
