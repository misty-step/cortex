// ─── Models ─────────────────────────────────────────────────────────────────
// Model config and status
// Implemented in PR 5

export function Models() {
  return (
    <div>
      <h2 className="text-lg font-semibold text-[var(--fg2)] mb-4">Models</h2>
      <p className="text-[var(--fg3)]">Models view implemented in PR 5.</p>
    </div>
  );
}
